olanguage: csharp
solution: EscherAuth.sln

sudo: false

addons:
  apt:
    packages:
      - nunit-console
      - mono-xbuild

script:
  - mono-xbuild /p:Configuration=Release solution-name.sln
  - nunit-console ./EscherAuthTests\bin\Release\EscherAuthTests.dll

